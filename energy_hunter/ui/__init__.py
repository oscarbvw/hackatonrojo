"""UI layer: Streamlit views and accessibility helpers.

Modules in this package are added in later tasks:

- ``layout``        — responsive grid helpers and theme injection (Task 11.1)
- ``a11y``          — ARIA-live status component (Task 11.2)
- ``building_form`` — add/edit building + phases form (Task 11.4)
- ``map_view``      — interactive map with anomaly overlay (Task 11.6)
- ``charts``        — live consumption and cost charts (Task 12.2)
- ``telemetry``     — per-phase telemetry display (Task 12.3)
- ``panic_panel``   — granular panic button panel (Task 13.2)
"""

"""Shared pytest fixtures for the Energy Hunter test suite.

Currently empty; fixtures (e.g., ``DictSessionStore``, fake clocks,
Flask ``test_client``s) are added in later tasks alongside the
modules they support.
"""
